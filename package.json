{
  "name": "duga-cloudflare-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "node --check src/worker.js && node --check src/analytics_worker.js && node --check src/request_logging_worker.js && node --check src/admin_single_message_worker.js && node --check src/launch_stats_worker.js && node --check src/production_worker.js && node --check src/animation_worker.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.1"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.0.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.33.1"
  }
}
